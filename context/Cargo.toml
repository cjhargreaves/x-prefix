[package]
name = "context"
version = "0.1.0"
edition = "2024"

[dependencies]
clients = { path = "../clients" }

[dev-dependencies]
reqwest = "0.12"
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
dotenvy = "0.15"
